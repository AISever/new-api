# 基础文本响应

## OpenAPI Specification

```yaml
openapi: 3.0.1
info:
  title: ''
  description: ''
  version: 1.0.0
paths:
  /responses:
    post:
      summary: 基础文本响应
      deprecated: false
      description: ''
      tags:
        - 模型接口/聊天(Chat)/OpenAI/Responses格式
      parameters:
        - name: Content-Type
          in: header
          description: ''
          required: true
          example: application/json
          schema:
            type: string
        - name: Accept
          in: header
          description: ''
          required: true
          example: application/json
          schema:
            type: string
        - name: Authorization
          in: header
          description: ''
          required: false
          example: Bearer {{YOUR_API_KEY}}
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              properties:
                model:
                  type: string
                  description: >-
                    用于生成响应的模型ID，例如 gpt-4.1 或 o3。OpenAI
                    提供各种具有不同能力、性能特性和价格点的模型。请参阅模型指南以浏览和比较可用模型。
                temperature:
                  type: number
                  description: >-
                    要使用的采样温度，介于 0 和 2
                    之间。较高的值（如0.8）会使输出更加随机，而较低的值（如0.2）会使其更加集中和确定性。我们通常建议更改此值或
                    top_p，但不要同时更改。
                  nullable: true
                top_p:
                  type: number
                  description: >-
                    一种替代采样温度的方法，称为核采样，其中模型考虑具有 top_p 概率质量的标记结果。因此，0.1 意味着只考虑包含前
                    10% 概率质量的标记。


                    我们通常建议更改此值或 temperature，但不要同时更改。
                  nullable: true
                stream:
                  type: boolean
                  description: >-
                    如果设置为
                    true，模型响应数据将在生成时通过服务器发送事件流式传输到客户端。请参阅下方的流式响应部分获取更多信息，以及流式响应指南了解如何处理流式事件。
                user:
                  type: string
                  description: 表示最终用户的唯一标识符，可以帮助 OpenAI 监控和检测滥用行为。
                store:
                  type: boolean
                  description: 是否存储此聊天补全请求的输出以用于我们的模型蒸馏或评估产品。
                  nullable: true
                metadata:
                  type: object
                  properties: {}
                  x-apifox-orders: []
                  description: |-
                    可以附加到对象的16个键值对集合。这对于以结构化格式存储对象的其他信息很有用,并可以通过 API 或仪表板查询对象。

                    键是最大长度为64个字符的字符串。值是最大长度为512个字符的字符串。
                  nullable: true
                service_tier:
                  type: string
                  description: >-
                    指定用于处理请求的延迟层级。此参数与订阅了 scale tier 服务的客户相关：


                    如果设置为 'auto'，且项目启用了 Scale tier，系统将使用 scale tier 信用直到用完

                    如果设置为 'auto'，且项目未启用 Scale tier，请求将使用默认服务层级处理，具有较低的正常运行时间 SLA
                    且无延迟保证

                    如果设置为 'default'，请求将使用默认服务层级处理，具有较低的正常运行时间 SLA 且无延迟保证

                    如果设置为 'flex'，请求将使用 Flex Processing 服务层级处理。详情请参阅文档。

                    未设置时，默认行为为 'auto'

                    当设置此参数时，响应体将包含使用的 service_tier
                  nullable: true
                tools:
                  type: array
                  items:
                    type: string
                  description: >-
                    模型可能调用的工具列表。目前仅支持函数作为工具。使用此参数提供模型可能生成 JSON 输入的函数列表。最多支持 128
                    个函数。
                tool_choice:
                  type: string
                  description: >-
                    控制模型调用哪个工具（如果有）： - none：模型不会调用任何工具，而是生成消息 -
                    auto：模型可以在生成消息或调用一个或多个工具之间选择 - required：模型必须调用一个或多个工具 -
                    {"type": "function", "function": {"name":
                    "my_function"}}：强制模型调用特定工具


                    当没有工具时默认为 none，有工具时默认为 auto
                parallel_tool_calls:
                  type: boolean
                  description: 是否在工具使用期间启用并行函数调用。
                input:
                  type: string
                  description: 供给模型的文本、图像或文件输入，用于生成响应。
                include:
                  type: array
                  items:
                    type: string
                  description: 指定要在模型响应中包含的附加输出数据。
                  nullable: true
                instructions:
                  type: string
                  description: >-
                    作为模型上下文中的第一项插入系统（或开发者）消息。


                    当与 previous_response_id
                    一起使用时，前一个响应中的指令不会被带到下一个响应。这使得在新响应中轻松切换系统（开发者）消息变得简单。
                  nullable: true
                max_output_tokens:
                  type: integer
                  description: 可以为响应生成的令牌数量的上限，包括可见输出令牌和推理令牌。
                  nullable: true
                previous_response_id:
                  type: string
                  description: 模型的前一个响应的唯一ID。使用此参数创建多轮对话。了解更多关于对话状态。
                  nullable: true
                reasoning:
                  type: object
                  properties: {}
                  x-apifox-orders: []
                  description: 仅适用于o系列模型
                  nullable: true
                text:
                  type: object
                  properties: {}
                  x-apifox-orders: []
                  description: >-
                    模型文本响应的配置选项。可以是纯文本或结构化JSON数据。配置 { "type": "json_schema" }
                    启用结构化输出，确保模型将匹配您提供的JSON模式。更多信息请参阅结构化输出指南。


                    默认格式为 { "type": "text" }，没有其他选项。


                    不推荐用于gpt-4o及更新的模型： 设置为 { "type": "json_object" }
                    启用较旧的JSON模式，确保模型生成的消息是有效的JSON。对于支持的模型，首选使用 json_schema。
                truncation:
                  type: string
                  description: 用于模型响应的截断策略：
                  nullable: true
              x-apifox-orders:
                - input
                - model
                - include
                - instructions
                - max_output_tokens
                - metadata
                - parallel_tool_calls
                - previous_response_id
                - reasoning
                - service_tier
                - store
                - stream
                - temperature
                - text
                - tool_choice
                - tools
                - top_p
                - truncation
                - user
              x-apifox-refs: {}
              required:
                - model
                - input
              $ref: '#/components/schemas/responses'
            example:
              model: gpt-4.1
              input:
                - role: user
                  content: Hello!
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  object:
                    type: string
                  created:
                    type: integer
                  choices:
                    type: array
                    items:
                      type: object
                      properties:
                        index:
                          type: integer
                        message:
                          type: object
                          properties:
                            role:
                              type: string
                            content:
                              type: string
                          required:
                            - role
                            - content
                          x-apifox-orders:
                            - role
                            - content
                          x-apifox-ignore-properties: []
                        finish_reason:
                          type: string
                      x-apifox-orders:
                        - index
                        - message
                        - finish_reason
                      x-apifox-ignore-properties: []
                  usage:
                    type: object
                    properties:
                      prompt_tokens:
                        type: integer
                      completion_tokens:
                        type: integer
                      total_tokens:
                        type: integer
                    required:
                      - prompt_tokens
                      - completion_tokens
                      - total_tokens
                    x-apifox-orders:
                      - prompt_tokens
                      - completion_tokens
                      - total_tokens
                    x-apifox-ignore-properties: []
                required:
                  - id
                  - object
                  - created
                  - choices
                  - usage
                x-apifox-orders:
                  - id
                  - object
                  - created
                  - choices
                  - usage
                x-apifox-ignore-properties: []
          headers: {}
          x-apifox-name: OK
      security: []
      x-apifox-folder: 模型接口/聊天(Chat)/OpenAI/Responses格式
      x-apifox-status: released
      x-run-in-apifox: https://app.apifox.com/web/project/3593080/apis/api-291767948-run
components:
  schemas:
    responses:
      type: object
      properties:
        input:
          type: string
          description: 供给模型的文本、图像或文件输入，用于生成响应。
        model:
          type: string
          description: >-
            用于生成响应的模型ID，例如 gpt-4.1 或 o3。OpenAI
            提供各种具有不同能力、性能特性和价格点的模型。请参阅模型指南以浏览和比较可用模型。
        include:
          type: array
          items:
            type: string
          description: 指定要在模型响应中包含的附加输出数据。
          nullable: true
        instructions:
          type: string
          description: >-
            作为模型上下文中的第一项插入系统（或开发者）消息。


            当与 previous_response_id
            一起使用时，前一个响应中的指令不会被带到下一个响应。这使得在新响应中轻松切换系统（开发者）消息变得简单。
          nullable: true
        max_output_tokens:
          type: integer
          description: 可以为响应生成的令牌数量的上限，包括可见输出令牌和推理令牌。
          nullable: true
        metadata:
          type: object
          properties: {}
          x-apifox-orders: []
          description: |-
            可以附加到对象的16个键值对集合。这对于以结构化格式存储对象的其他信息很有用,并可以通过 API 或仪表板查询对象。

            键是最大长度为64个字符的字符串。值是最大长度为512个字符的字符串。
          x-apifox-ignore-properties: []
          nullable: true
        parallel_tool_calls:
          type: boolean
          description: 是否在工具使用期间启用并行函数调用。
        previous_response_id:
          type: string
          description: 模型的前一个响应的唯一ID。使用此参数创建多轮对话。了解更多关于对话状态。
          nullable: true
        reasoning:
          type: object
          properties: {}
          x-apifox-orders: []
          description: 仅适用于o系列模型
          x-apifox-ignore-properties: []
          nullable: true
        service_tier:
          type: string
          description: |-
            指定用于处理请求的延迟层级。此参数与订阅了 scale tier 服务的客户相关：

            如果设置为 'auto'，且项目启用了 Scale tier，系统将使用 scale tier 信用直到用完
            如果设置为 'auto'，且项目未启用 Scale tier，请求将使用默认服务层级处理，具有较低的正常运行时间 SLA 且无延迟保证
            如果设置为 'default'，请求将使用默认服务层级处理，具有较低的正常运行时间 SLA 且无延迟保证
            如果设置为 'flex'，请求将使用 Flex Processing 服务层级处理。详情请参阅文档。
            未设置时，默认行为为 'auto'
            当设置此参数时，响应体将包含使用的 service_tier
          nullable: true
        store:
          type: boolean
          description: 是否存储此聊天补全请求的输出以用于我们的模型蒸馏或评估产品。
          nullable: true
        stream:
          type: boolean
          description: >-
            如果设置为
            true，模型响应数据将在生成时通过服务器发送事件流式传输到客户端。请参阅下方的流式响应部分获取更多信息，以及流式响应指南了解如何处理流式事件。
        temperature:
          type: number
          description: >-
            要使用的采样温度，介于 0 和 2
            之间。较高的值（如0.8）会使输出更加随机，而较低的值（如0.2）会使其更加集中和确定性。我们通常建议更改此值或
            top_p，但不要同时更改。
          nullable: true
        text:
          type: object
          properties: {}
          x-apifox-orders: []
          description: >-
            模型文本响应的配置选项。可以是纯文本或结构化JSON数据。配置 { "type": "json_schema" }
            启用结构化输出，确保模型将匹配您提供的JSON模式。更多信息请参阅结构化输出指南。


            默认格式为 { "type": "text" }，没有其他选项。


            不推荐用于gpt-4o及更新的模型： 设置为 { "type": "json_object" }
            启用较旧的JSON模式，确保模型生成的消息是有效的JSON。对于支持的模型，首选使用 json_schema。
          x-apifox-ignore-properties: []
        tool_choice:
          type: string
          description: >-
            控制模型调用哪个工具（如果有）： - none：模型不会调用任何工具，而是生成消息 -
            auto：模型可以在生成消息或调用一个或多个工具之间选择 - required：模型必须调用一个或多个工具 - {"type":
            "function", "function": {"name": "my_function"}}：强制模型调用特定工具


            当没有工具时默认为 none，有工具时默认为 auto
        tools:
          type: array
          items:
            type: string
          description: 模型可能调用的工具列表。目前仅支持函数作为工具。使用此参数提供模型可能生成 JSON 输入的函数列表。最多支持 128 个函数。
        top_p:
          type: number
          description: >-
            一种替代采样温度的方法，称为核采样，其中模型考虑具有 top_p 概率质量的标记结果。因此，0.1 意味着只考虑包含前 10%
            概率质量的标记。


            我们通常建议更改此值或 temperature，但不要同时更改。
          nullable: true
        truncation:
          type: string
          description: 用于模型响应的截断策略：
          nullable: true
        user:
          type: string
          description: 表示最终用户的唯一标识符，可以帮助 OpenAI 监控和检测滥用行为。
      x-apifox-orders:
        - input
        - model
        - include
        - instructions
        - max_output_tokens
        - metadata
        - parallel_tool_calls
        - previous_response_id
        - reasoning
        - service_tier
        - store
        - stream
        - temperature
        - text
        - tool_choice
        - tools
        - top_p
        - truncation
        - user
      x-apifox-refs: {}
      required:
        - input
        - model
      x-apifox-ignore-properties: []
      x-apifox-folder: ''
  securitySchemes: {}
servers:
  - url: https://corp-api.aisever.cn/v1
    description: 主站接口①
  - url: https://us.ifopen.ai/v1
    description: 主站接口②
  - url: https://35.aigcbest.top/v1
    description: Beta站接口①
security: []

```
